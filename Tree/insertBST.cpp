#include<iostream>
using namespace std;

int main()
{
    // struct node* root=new node(5);
    while(1){
        cout<<"Enter 0 to exit";
        int ch;
        cin>>ch;
        if(ch==0) break;
        cout<<"Enter Element: ";
        int e;
        cin>>e;
        // insert(root);
    }
    return 0;
}